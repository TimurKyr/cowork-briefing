# COWORK.md — контракт утреннего обновления «Мой день»

Это самодостаточное задание для облачного агента (Claude Cowork). Открой эту
папку в Cowork и поставь **облачную повторяющуюся задачу на каждое утро ~07:00
по Asia/Almaty (UTC+5)**.

> **Часовой пояс.** Всё «сегодня» считается по **Asia/Almaty**. Дата в запросах —
> `YYYY-MM-DD` по Алматы, не по UTC. Сайт считает «сегодня» так же.

> **Ключевое требование.** Вся работа — это чистые HTTP-запросы к внешним API
> (Google Calendar, погода, Supabase REST). **Никаких локальных файлов, скриптов
> или запущенного сервера не требуется.** Задача должна выполняться в облаке при
> закрытом ноутбуке.

---

## Что нужно сделать каждое утро

1. **Календарь.** Взять события Google Calendar на **сегодня** (по моему часовому
   поясу): для каждого события — время начала, время конца, название и `location`
   (если указан). События на весь день можно пропустить или добавить без времени.

2. **Погода.** Получить краткий прогноз погоды на сегодня для моего города
   (короткая строка вида `«переменная облачность, +24°»`).

3. **Фокус и заметка.** Сформировать:
   - `focus` — один главный приоритет дня (короткая фраза);
   - `note` — 1–2 спокойных предложения-напутствия (необязательно).

4. **Записать день в Supabase.** Сделать **upsert** строки в таблицу `days` за
   сегодня одним HTTP-запросом (см. ниже), обязательно проставив
   **`updated_at` = текущее время** (ISO 8601, напр. `2026-07-16T07:00:00+05:00`).
   Ключ идемпотентности — `date` (PK), повторный запуск перезапишет строку.

5. **Перенести невыполненные дела.** Одним запросом `PATCH` перевести все задачи
   с прошлых дат, что ещё не сделаны, на сегодня и пометить их перенесёнными:
   `date < сегодня AND done = false` → `date = сегодня, carried_over = true`
   (см. запрос ниже). Сайт и без этого показывает такие задачи, но этот шаг
   «подчищает» таблицу.

6. **(Опционально) Дедлайны.** Если в календаре есть события-дедлайны/сдачи
   (экзамен, дедлайн проекта, оплата) — можно добавить их в таблицу `deadlines`
   (см. ниже). Не дублируй уже существующие (сначала проверь `GET`).

Сами отметки в чеклисте (`done`) утреннее обновление не меняет — их веду я на сайте.

---

## Параметры (заполни своими)

| Что | Значение |
|-----|----------|
| `SUPABASE_URL` | `https://YOUR-PROJECT.supabase.co` |
| `SUPABASE_ANON_KEY` | `YOUR-ANON-KEY` (только anon/public ключ) |
| Город для погоды | Алматы |
| Часовой пояс | **Asia/Almaty** (UTC+5) |

> Используем **только anon-ключ**. Он публичен по назначению. Никогда не
> используй `service_role`.

---

## Точный вид запроса upsert в `days`

`date` в теле — **сегодняшняя** дата в формате `YYYY-MM-DD` (локальная).
`timeline` — **JSON-массив объектов** (jsonb), каждый: `{start, end, title, location, kind}`.
- `start`/`end` — строки `"HH:MM"` (24 ч). Если конца нет — можно опустить `end`.
- `location` — строка или опустить, если места нет.
- `kind` — одно из: `work`, `study`, `sport`, `break`, `hobby`, `other`
  (определяет цвет блока; незнакомое значение отрисуется как `other`).

```http
POST https://YOUR-PROJECT.supabase.co/rest/v1/days
apikey: YOUR-ANON-KEY
Authorization: Bearer YOUR-ANON-KEY
Content-Type: application/json
Prefer: resolution=merge-duplicates
```

Тело запроса:

```json
{
  "date": "2026-07-16",
  "focus": "Досдать лабу по базам данных до 18:00",
  "note": "Один настоящий приоритет сегодня — лаба. Не хватай лишнего.",
  "weather": "переменная облачность, +24°",
  "updated_at": "2026-07-16T07:00:00+05:00",
  "timeline": [
    { "start": "08:00", "end": "12:00", "title": "Работа — Nomad Insurance", "location": "офис, Абая 12", "kind": "work" },
    { "start": "12:30", "end": "14:00", "title": "Обед и отдых", "kind": "break" },
    { "start": "14:00", "end": "16:00", "title": "Учёба — лаба по БД", "location": "дом", "kind": "study" },
    { "start": "17:00", "end": "18:30", "title": "Теннис", "location": "корты Достык", "kind": "sport" },
    { "start": "20:00", "end": "21:00", "title": "Пианино", "kind": "hobby" }
  ]
}
```

Эквивалент в виде `curl` (для проверки вручную):

```bash
curl -sS -X POST "https://YOUR-PROJECT.supabase.co/rest/v1/days" \
  -H "apikey: YOUR-ANON-KEY" \
  -H "Authorization: Bearer YOUR-ANON-KEY" \
  -H "Content-Type: application/json" \
  -H "Prefer: resolution=merge-duplicates" \
  -d '{
    "date": "2026-07-16",
    "focus": "…",
    "note": "…",
    "weather": "…",
    "timeline": [ { "start": "08:00", "end": "12:00", "title": "…", "location": "…", "kind": "work" } ]
  }'
```

### Почему именно так
- **`Prefer: resolution=merge-duplicates`** превращает обычный `POST` в upsert:
  если строка с таким `date` уже есть, она перезаписывается, а не даёт ошибку
  дубликата PK. Ровно то, что нужно для «переписать сегодняшний день заново».
- **`timeline` отправляется как настоящий JSON-массив** (не строка) — колонка
  имеет тип `jsonb`, сайт читает её как массив блоков.
- **`updated_at`** — сайт показывает в футере «обновлено в HH:MM» (по Алматы).
- Хочешь получить записанную строку в ответе — добавь к заголовку:
  `Prefer: resolution=merge-duplicates,return=representation`.

---

## Шаг 5 — перенос невыполненных задач

Один `PATCH` по фильтру: всё с прошлых дат и `done=false` → на сегодня, помечаем
`carried_over=true`. `2026-07-16` замени на сегодняшнюю дату (Алматы).

```bash
curl -sS -X PATCH \
  "https://YOUR-PROJECT.supabase.co/rest/v1/tasks?date=lt.2026-07-16&done=eq.false" \
  -H "apikey: YOUR-ANON-KEY" \
  -H "Authorization: Bearer YOUR-ANON-KEY" \
  -H "Content-Type: application/json" \
  -H "Prefer: return=minimal" \
  -d '{ "date": "2026-07-16", "carried_over": true }'
```

- Фильтр `date=lt.<сегодня>&done=eq.false` выбирает только просроченные незакрытые.
- Тело меняет у них `date` на сегодня и ставит `carried_over=true`.
- Идемпотентно: если переносить нечего — PATCH просто ничего не затронет.

---

## Шаг 6 (опционально) — дедлайны

Постоянный список, не привязан к дню. Перед вставкой можно проверить, нет ли уже
такого (`GET /rest/v1/deadlines?title=eq.<...>`), чтобы не задваивать.

```bash
curl -sS -X POST "https://YOUR-PROJECT.supabase.co/rest/v1/deadlines" \
  -H "apikey: YOUR-ANON-KEY" \
  -H "Authorization: Bearer YOUR-ANON-KEY" \
  -H "Content-Type: application/json" \
  -H "Prefer: return=minimal" \
  -d '[{ "title": "Сдать проект по вебу", "due_date": "2026-07-20" }]'
```

- `due_date` — `YYYY-MM-DD`. Обратный отсчёт сайт считает сам от сегодняшней даты.
- Закрытие дедлайна (`done=true`) я делаю тапом на сайте — агенту трогать не нужно.

---

## Критерий успеха
- Ответы Supabase — `2xx`.
- На сайте видны сегодняшний фокус/погода/таймлайн, «обновлено в HH:MM» в футере,
  строка «сейчас/дальше», перенесённые задачи (с подписью «⤷ с вчера») и дедлайны
  с обратным отсчётом.
- Повторный запуск за тот же день не плодит дубликаты (days upsert, tasks-перенос
  идемпотентны).
